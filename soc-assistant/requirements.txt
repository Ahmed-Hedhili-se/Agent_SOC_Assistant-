# Core LLM / agent frameworks
langchain-core>=0.2.0
langchain-community>=0.2.0
langchain-openai>=0.1.0        # ChatOpenAI client for any OpenAI-compatible endpoint (Ollama, vLLM, Grok)
langchain-huggingface>=0.0.3   # sentence-transformer embeddings for the RAG stores
langgraph>=0.2.0

# Vector stores & embeddings
chromadb>=0.5.0
langchain-chroma>=0.1.4
sentence-transformers>=3.0.0

# API / networking
fastapi>=0.111.0
uvicorn>=0.30.0
requests>=2.32.0

# Data validation & parsing
pydantic>=2.7.0
PyYAML>=6.0.1

# Testing
pytest>=8.0.0
httpx>=0.27.0                  # required by fastapi.testclient

# DPO fine-tuning (optional -- training/dpo_train.py)
# train_role() degrades to a clear skip message if these aren't installed,
# so the rest of the pipeline works without them.
torch>=2.2.0
transformers>=4.42.0
trl>=0.9.4
datasets>=2.19.0
accelerate>=0.30.0
